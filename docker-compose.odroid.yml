# DEPRECATED historical 13번 definition. Do not run Docker on 192.168.1.13.
# Current Docker/PostgreSQL deployment uses docker-compose.yml on 192.168.1.14.
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    env_file:
      - .env.odroid
    environment:
      DATABASE_URL: "${DATABASE_URL:-sqlite+aiosqlite:////app/data/app.sqlite3}"
      RELEASE_SHA: "${RELEASE_SHA:-unknown}"
      ENABLE_SCHEDULER: "${ENABLE_SCHEDULER:-true}"
      SEED_SAMPLE_DATA: "${SEED_SAMPLE_DATA:-false}"
      USE_SAMPLE_CLIENT_WHEN_NO_KEY: "${USE_SAMPLE_CLIENT_WHEN_NO_KEY:-false}"
      ENABLE_INCHEON_COLLECTION: "${ENABLE_INCHEON_COLLECTION:-true}"
      ENABLE_INCHEON_FEE_COLLECTION: "${ENABLE_INCHEON_FEE_COLLECTION:-true}"
      ENABLE_FEE_COLLECTION: "${ENABLE_FEE_COLLECTION:-false}"
      COLLECT_INTERVAL_SECONDS: "${COLLECT_INTERVAL_SECONDS:-600}"
      MANUAL_COLLECT_MIN_INTERVAL_SECONDS: "${MANUAL_COLLECT_MIN_INTERVAL_SECONDS:-600}"
      UPSTREAM_RATE_LIMIT_BACKOFF_SECONDS: "${UPSTREAM_RATE_LIMIT_BACKOFF_SECONDS:-3600}"
      API_TIMEOUT_SECONDS: "${API_TIMEOUT_SECONDS:-15}"
      HOLIDAY_CACHE_SECONDS: "${HOLIDAY_CACHE_SECONDS:-86400}"
      AIRPORT_CODES_CSV: "${AIRPORT_CODES_CSV:-GMP,PUS,CJU,ICN}"
      CORS_ORIGINS_CSV: "${CORS_ORIGINS_CSV:-http://192.168.1.13:3000,https://pr2.digitie.mywire.org,http://localhost:3000}"
      TRUSTED_HOSTS_CSV: "${TRUSTED_HOSTS_CSV:-192.168.1.13,pr2.digitie.mywire.org,localhost,127.0.0.1,testserver,backend}"
      ENABLE_API_DOCS: "${ENABLE_API_DOCS:-false}"
      APP_TIMEZONE: "${APP_TIMEZONE:-Asia/Seoul}"
      DATA_GO_KR_SERVICE_KEY: "${DATA_GO_KR_SERVICE_KEY:-}"
    volumes:
      - parking_radar_backend_data:/app/data
    ports:
      - "${PUBLIC_API_PORT:-8000}:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5).read()"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: "${NEXT_PUBLIC_API_BASE_URL:-}"
        NEXT_PUBLIC_API_PORT: "${NEXT_PUBLIC_API_PORT:-8000}"
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    environment:
      BACKEND_INTERNAL_URL: "${BACKEND_INTERNAL_URL:-http://backend:8000}"
    ports:
      - "${PUBLIC_WEB_PORT:-3000}:3000"

volumes:
  parking_radar_backend_data:
